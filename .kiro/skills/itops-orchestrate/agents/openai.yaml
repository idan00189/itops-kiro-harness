interface:
  display_name: "ITOps Full Investigation"
  short_description: "Run full read-only incident investigations"
  default_prompt: "Use $itops-orchestrate to perform a full cross-system investigation and write the Hebrew Markdown report."
