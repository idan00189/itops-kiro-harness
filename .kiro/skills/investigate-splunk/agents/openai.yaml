interface:
  display_name: "Investigate Splunk"
  short_description: "Investigate logs and draft Splunk dashboard XML"
  default_prompt: "Use $investigate-splunk to examine the supplied incident window with bounded read-only searches."
