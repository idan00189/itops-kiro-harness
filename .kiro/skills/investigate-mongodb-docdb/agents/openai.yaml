interface:
  display_name: "Investigate MongoDB and DocumentDB"
  short_description: "Inspect named document connections safely"
  default_prompt: "Use $investigate-mongodb-docdb to select the named URI and authorized database, then inspect this incident with bounded read-only queries."
