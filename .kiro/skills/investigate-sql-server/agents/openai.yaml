interface:
  display_name: "Investigate SQL Server"
  short_description: "Query named SQL Server replicas safely"
  default_prompt: "Use $investigate-sql-server to select the named connection and answer the targeted question from its verified read-only secondary."
