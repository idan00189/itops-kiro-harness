interface:
  display_name: "Investigate SQL Server"
  short_description: "Query a SQL Server replica safely"
  default_prompt: "Use $investigate-sql-server to answer the targeted question from a verified read-only SQL Server secondary."
