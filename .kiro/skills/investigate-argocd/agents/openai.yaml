interface:
  display_name: "Investigate Argo CD"
  short_description: "Inspect GitOps application health and drift safely"
  default_prompt: "Use $investigate-argocd to inspect this incident through read-only application and resource evidence."
